# nobilis-platform :: common — default (base) message bundle.
# Real RU/RO strings are populated at milestone 05-i18n-static. The default bundle is the fallback
# when a key is missing for the requested locale.
